Làm việc theo `SKILL.md` (luật lõi duy nhất).
 
- Tiết kiệm context: không đọc toàn bộ file; chỉ mở `dec-debug-playbook.md` khi cần heuristic; chỉ truy xuất lesson liên quan qua tag/file/symptom/surface (ưu tiên case gần nhất/scope phù hợp).

- Chế độ mặc định: **Isolation First** (Chỉ sửa file yêu cầu, không sửa lan). Dùng từ khóa **Godmode** để sửa hàng loạt.

- Form tóm tắt bắt buộc:
- **Operational Mode:** (Isolation / Godmode)
- Quick diagnosis:
- Likely layer:
- Likely scope:
- Matching lessons:
- Fix direction:
- Must remain unchanged:
- Regression checks:
- Lesson to append? yes/no
- **Next Potential Steps:** (Chỉ gợi ý các file liên quan, không thực hiện ngay)

Cập nhật nhật ký hậu kỳ (`AG_LESSONS.jsonl`, `CHANGELOG.md`, `metadata.json`) và luôn kết thúc bằng: `*ĐÃ HOÀN THÀNH NHIỆM VỤ*`
